
<div class="container-fluid">
  <div class="row d-flex justify-content-center">
    <div class="col-sm-12 col-md-12 col-lg-9">
      <%= link_to  mypage_users_path, class: 'mypage-link', title: 'マイページ' do %>
        <i class="fa-solid fa-address-card"></i>
      <% end %>＞
      <%= link_to new_category_path, class: 'return-link-brown', title: '１つ前に戻る', data: { "turbolinks" => false } do %>
        <i class="fa-solid fa-book-open"></i>
      <% end %>＞
    </div>
  </div>

  <div class="row d-flex justify-content-center">
    <div class="col-sm-12 col-md-12 col-lg-5">
      <div class="d-flex justify-content-center align-items-center w-100 mb-2">
        <%= form_with(model: [@category, @title], url: category_titles_path(@category), method: :post, local: true, class: 'w-100') do |f| %>
          <div class="row d-flex justify-content-center">
            <div class="col-sm-12 col-md-8 col-lg-8 mb-2">
              <%= f.text_field :title_name, class: "form-control mr-1", id: 'title_name', autofocus: true, placeholder: "もくじ２を入力(20文字まで登録できます※必須)" %>
            </div>
            <div class="col-sm-8 col-md-4 col-lg-4 mb-2 d-flex justify-content-center">
              <%= f.submit "項目を追加", class: "btn btn-primary w-100", title: 'フォームに入力してこのボタンで保存します(保存をすると保存順に切り替わります)' %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="row d-flex justify-content-center">
    <div class="col-sm-12 col-md-12 col-lg-9">
      <span class="category-name"><%= @category.category_name %></span>
      <%= link_to '五十音順', category_path(sort_order: 'alphabetical'),title:
'0(半角数字)
０(全角数字)a(半角小文字)
ａ(全角小文字)
A(半角大文字)
Ａ(全角大文字)
ｱ(半角カナ)
ァ(全角カナ)
ぁ(かな)
漢字
その他の順で並びます
' %>
      |
      <%= link_to '登録順', category_path(sort_order: 'created_at'), title: '保存順で並びます' %>
      <%= render 'title_save_oder', sorted_titles: @sorted_titles, category: @category %>
    </div>
  </div>
</div>

<%= javascript_pack_tag 'title_name_edit', 'data-turbolinks-track': 'reload' %>












