<%= stylesheet_link_tag 'public/users', media: 'all' %>
<div class="container-fluid">
  <div class="row d-flex">
    <div class="col-md-2 d-flex flex-column align-items-center">
      <%= render 'public/users/user_card', user: @user, followings_count: @followings_count, followers_count: @followers_count %>
    </div>
    <div class="col-md-10">
      <h4><%= @user.nick_name %>さんがフォローしているUser</h4>
      <% if @followings.any? %>
        <div id="admin-users-list" class="row">
          <% @followings.each_slice(5) do |followings_slice| %>
            <% followings_slice.each do |user| %>
              <%= render 'user_card_follors', user: user %>
            <% end %>
          <% end %>
        </div>
        <div class="pagination">
          <%= paginate @followings %>
        </div>
      <% else %>
        <p>フォローしているユーザーはいません。</p>
      <% end %>
    </div>
  </div>
</div>